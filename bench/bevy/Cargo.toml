[package]
name = "terrain-bench"
version = "0.1.0"
edition = "2024"
description = "Bevy side of the Unity/Bevy terrain benchmark (bench/scene.json)"

[dependencies]
bevy = "0.19.1"
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"

[features]
webgpu = ["bevy/webgpu"]
